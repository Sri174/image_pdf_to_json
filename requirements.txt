fastapi>=0.95.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
Pillow>=9.5.0
pdfplumber>=0.7.6
pdf2image>=1.16.3
requests>=2.31.0
pytest>=7.0.0

# Note: heavy native libraries (zbar, system poppler/tesseract) are not
# required at install-time. The Dockerfile installs `poppler-utils` and
# `tesseract-ocr` so OCR and PDF->image conversion work in the container.

# API server
fastapi>=0.95.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
